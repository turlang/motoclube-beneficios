services:
  - type: web
    name: motoclube-beneficios-api
    runtime: node
    plan: free
    region: virginia
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    autoDeployTrigger: off
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: QR_HMAC_SECRET
        generateValue: true
      - key: FRONTEND_URL
        value: https://motoclube-beneficios-web.onrender.com
      - key: PAYMENT_WEBHOOK_SECRET
        generateValue: true
      - key: COOKIE_SAME_SITE
        value: none
      - key: PARTNER_JWT_EXPIRES_IN
        value: 10h
      - key: QR_TTL_SECONDS
        value: "60"

  - type: web
    name: motoclube-beneficios-web
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    autoDeployTrigger: off
    envVars:
      - key: VITE_API_URL
        value: https://motoclube-beneficios-api.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
