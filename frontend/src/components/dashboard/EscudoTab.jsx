import { Clock3, RefreshCw, ShieldAlert } from "lucide-react";
import { DigitalPatch } from "../DigitalPatch.jsx";
import { InfoStat } from "./DashboardUI.jsx";
export function EscudoTab({ user, isActive, qr, loadingQr, qrError, secondsRemaining, onRefresh }) {
  return <><DigitalPatch user={user} active={isActive}><div className="mt-4 rounded-[1.75rem] border border-amber-400/15 bg-black/45 p-4">{isActive ? <><div className="mb-4 flex items-center justify-between gap-3"><div><p className="text-[10px] font-black uppercase tracking-[0.28em] text-zinc-300">Validação do benefício</p><p className="mt-1 text-[11px] text-zinc-600">Mostre este QR ao parceiro credenciado</p></div><button type="button" onClick={onRefresh} disabled={loadingQr} className="grid h-10 w-10 shrink-0 place-items-center rounded-xl border border-amber-400/15 bg-zinc-950 text-amber-300 transition active:scale-95 disabled:opacity-40" aria-label="Atualizar QR Code"><RefreshCw className={["h-4 w-4",loadingQr?"animate-spin":""].join(" ")}/></button></div><div className="qr-frame mx-auto w-fit rounded-[1.6rem] p-3">{qr?.imageDataUrl?<img src={qr.imageDataUrl} alt="QR Code dinâmico do associado" width="260" height="260" className="h-52 w-52 rounded-xl object-contain sm:h-60 sm:w-60"/>:<div className="grid h-52 w-52 place-items-center rounded-xl bg-zinc-200 text-sm font-black uppercase text-zinc-500 sm:h-60 sm:w-60">{loadingQr?"Gerando escudo...":"QR indisponível"}</div>}</div><div className="mt-4 flex items-center justify-center gap-2 text-xs text-zinc-500"><Clock3 className="h-4 w-4 text-amber-400"/>Atualiza em <span className="min-w-9 font-black tabular-nums text-amber-300">{secondsRemaining}s</span></div></>:<div className="py-7 text-center"><div className="mx-auto grid h-14 w-14 place-items-center rounded-full border border-zinc-800 bg-zinc-950 text-zinc-600"><ShieldAlert className="h-7 w-7"/></div><h4 className="mt-4 text-lg font-black uppercase text-zinc-400">QR oculto por segurança</h4><p className="mx-auto mt-2 max-w-xs text-sm leading-6 text-zinc-600">A assinatura está inativa. O QR só volta a ser emitido quando a situação do associado estiver regularizada.</p></div>}{qrError&&<p className="mt-4 rounded-2xl border border-red-500/20 bg-red-500/10 px-4 py-3 text-sm text-red-300">{qrError}</p>}</div></DigitalPatch><section className="grid grid-cols-2 gap-3"><InfoStat title="Validação" value={isActive?"Liberada":"Bloqueada"}/><InfoStat title="Código" value={isActive?"Rotativo • HMAC":"Suspenso"}/></section></>;
}
